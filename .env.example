# LLM Configuration
# Groq API is used for both local development and deployment
# Get your API key at: https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# Food Classification API
# Google Cloud Vision API (required - 1000 free requests/month)
# Get your API key at: https://console.cloud.google.com/apis/credentials
# CNN (TensorFlow.js) is used as fallback if Vision fails or is unset
GOOGLE_CLOUD_VISION_API_KEY=your_google_cloud_vision_api_key_here

# Database (production)
# Optional locally; set for Vercel/Neon etc. to use Postgres instead of SQLite
# POSTGRES_URL=postgresql://user:pass@host/db?sslmode=require

# Application
NODE_ENV=development
PORT=3000
